{
  "type": "module",
  "scripts": {
    "bundle": "esbuild index.ts --target=node16 --format=esm --bundle --outfile=bundle.js --platform=node",
    "bundle:paths": "yarn bundle --tsconfig=tsconfig.paths.json"
  },
  "dependencies": {
    "@mapbox/polyline": "^1.1.1",
    "@types/mapbox__polyline": "^1.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.23",
    "typescript": "^4.6.2"
  },
  "private": true
}
